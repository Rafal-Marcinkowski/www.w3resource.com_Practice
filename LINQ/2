//2.Write a program in C# Sharp to find the +ve numbers from a list of numbers using two where conditions in LINQ Query.
//Expected Output:
//The numbers within the range of 1 to 11 are :
//1 3 6 9 10

int[] initialArray = [-17, 3 - 0, 4, 7, 12, 11, -3, -11, 5, 1, -1];
var filteredArray = initialArray.Where(q => q > 0).Where(q => q >= 1 && q <= 11);
foreach (var item in filteredArray)
{
    Console.Write(item + " ");
}
